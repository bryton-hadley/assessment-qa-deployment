const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
    
    test('Return an Array', () => {

        expect((shuffleArray).toBe([]))
    })

    test('Should return an legth')
})